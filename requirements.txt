# requirements.txt
# Federated Learning for Type 2 Diabetes Prediction
# ─────────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# Python:   3.9 or 3.10 recommended

# ── Core ML ───────────────────────────────────────
torch>=2.0.0,<2.4.0
torchvision>=0.15.0        # needed by some torch utilities

# ── Federated Learning ────────────────────────────
flwr>=1.5.0,<2.0.0         # Flower FL framework (NumPyClient API)

# ── Data processing ───────────────────────────────
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0        # StandardScaler, metrics, train_test_split

# ── Serialisation ─────────────────────────────────
joblib>=1.3.0              # scaler persistence

# ── Visualisation ─────────────────────────────────
matplotlib>=3.7.0

# ── Experiment tracking (optional but recommended) ─
tensorboard>=2.13.0        # view with: tensorboard --logdir experiments/runs
# wandb>=0.15.0            # alternative: Weights & Biases (uncomment if preferred)

# ── Utilities ─────────────────────────────────────
tqdm>=4.65.0               # progress bars (used in some utilities)
